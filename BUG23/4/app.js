// ليك شرح التعبير العادي re مع تقسيمه إلى أجزاء، وإضافة تعليقات توضح وظيفة كل جزء:

// javascript
// نسخ
// تحرير
// let re = /https?:\/\/(?:[-\w]+\.)?([-\w]+)\.\w+(?:\.\w+)?\/?.*/i;
// تحليل التعبير العادي:
// javascript
// نسخ
// تحرير
// /https?:\/\//
// 🔹 https? → يطابق http أو https (s اختياري).
// 🔹 :\/\/ → يطابق :// حرفيًا، ويمثل بداية الرابط.

// javascript
// نسخ
// تحرير
// (?:[-\w]+\.)?
// 🔹 (?: ... ) → مجموعة غير ملتقطة (Non-capturing group)، لا تخزن القيم.
// 🔹 [-\w]+ → يطابق حروفًا (a-z, A-Z), أرقامًا (0-9), شرطات (-), أو شرطات سفلية (_).
// 🔹 \. → يطابق النقطة (.) التي تفصل نطاقات المستوى الأعلى (TLD).
// 🔹 ? → يجعل المجموعة السابقة ((?:[-\w]+\.)) اختيارية (قد تكون موجودة أو لا).

// 📌 وظيفة هذا الجزء:

// يطابق النطاق الفرعي (subdomain) إذا وُجد، مثل www. أو blog. في "https://www.example.com".
// إذا لم يكن هناك نطاق فرعي، لا يؤثر على النمط.
// javascript
// نسخ
// تحرير
// ([-\w]+)
// 🔹 [-\w]+ → يطابق اسم النطاق الرئيسي (domain name)، مثل "example" في "https://example.com".
// 🔹 ( ... ) → مجموعة ملتقطة (Capturing group)، مما يعني أن القيم المطابقة يمكن استخراجها.

// javascript
// نسخ
// تحرير
// \.\w+
// 🔹 \. → يطابق النقطة (.) التي تفصل بين النطاق وامتداده.
// 🔹 \w+ → يطابق امتداد النطاق مثل "com", "net", "org", إلخ.

// 📌 وظيفة هذا الجزء:

// يضمن أن هناك امتدادًا للنطاق، مثل "com" في "example.com".
// javascript
// نسخ
// تحرير
// (?:\.\w+)?
// 🔹 (?: ... )? → مجموعة غير ملتقطة، لكنها اختيارية.
// 🔹 \.\w+ → يطابق امتدادًا إضافيًا مثل "co.uk" في "example.co.uk".

// 📌 وظيفة هذا الجزء:

// يسمح بوجود امتداد نطاق من مستويين مثل "co.uk" أو "com.br".
// javascript
// نسخ
// تحرير
// \/?.*
// 🔹 \/? → يطابق / اختياريًا (قد يكون هناك مسار في الرابط أو لا).
// 🔹 .* → يطابق أي عدد من الأحرف بعد /، بما في ذلك المسارات والمعاملات.

// 📌 وظيفة هذا الجزء:

// يسمح بوجود مسار في الرابط مثل /page في "https://example.com/page".
// إذا لم يكن هناك مسار، لا يؤثر على النمط.
// javascript
// نسخ
// تحرير
// /i
// 🔹 i → يُعطِّل حساسية الأحرف (case-insensitive)، أي أن "HTTP" و "http" سيتم اعتبارهما متطابقين.

// ملخص:
// 🔹 هذا التعبير العادي يستخدم لاستخراج اسم النطاق الرئيسي من رابط URL، سواء كان يحتوي على نطاق فرعي أم لا.
// 🔹 يطابق:
// ✅ "https://www.google.com"
// ✅ "http://example.co.uk/path"
// ✅ "https://sub.domain.net"
// 🔹 لا يطابق:
// ❌ "ftp://example.com" (لأنه ليس http أو https).
// ❌ "example.com" (لأنه لا يحتوي على http:// أو https://).

// 📌 إذا كنت بحاجة إلى تعديل أو توضيح أكثر، أخبرني بذلك! 😊
